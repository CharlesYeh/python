Generators	- create a lambda, and pass it inside a CGenerator
Inheritance	- when creating a class def
	- also need to keep copies of super classes for super()
Continue/Break  - creating a built-in loop thingy that recurses in racket
Matching Excepts- match class names

string slices: desugar into loops?
LOOPS??!

